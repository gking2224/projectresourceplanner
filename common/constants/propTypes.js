import React, { Component, PropTypes } from 'react'

export const sessionInfo = React.PropTypes.object.isRequired
  // ({
  //   loggedOnUser: React.PropTypes.shape({
  //     id: React.PropTypes.string,
  //     firstName: React.PropTypes.string,
  //     surname: React.PropTypes.string,
  //     roles: React.PropTypes.arrayOf(React.PropTypes.string)
  //   }),
  //   permissions: React.PropTypes.arrayOf(React.PropTypes.string)
  // })
// }
