import * as ActionTypes from '../constants/actionTypes'

const resources = (
  state = {
    resourceList:[]
  },
  action) => {

  switch (action.type) {
    default:
      return state
  }
}

export default resources
