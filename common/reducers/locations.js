import * as ActionTypes from '../constants/actionTypes'

const locations = (
  state = {
    locationList:[]
  },
  action) => {

  switch (action.type) {
    default:
      return state
  }
}

export default locations
