import { ActionTypes } from '../constants'
import update from 'react-addons-update'

const DEFAULT_STATE = null

const projectWip = (state = DEFAULT_STATE, action) => {

  switch (action.type) {

    default:
      return state
  }
}

export default projectWip
